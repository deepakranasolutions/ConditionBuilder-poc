import { AdComponentHostDirective } from './ad-component-host.directive';

describe('AdComponentHostDirective', () => {
  it('should create an instance', () => {
    const directive = new AdComponentHostDirective();
    expect(directive).toBeTruthy();
  });
});
